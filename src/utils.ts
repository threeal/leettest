/**
 * Retrieves the build directory of the given file path.
 *
 * This function will return the build directory of the given file path.
 * If the file path is in the build directory, it will return the file path directory.
 *
 * @param filePath - The path of the file.
 * @returns The path of the build directory.
 */
export function getBuildDirOfFile(filePath: string): string {

}
